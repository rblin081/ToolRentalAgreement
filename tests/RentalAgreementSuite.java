package Cardinal.tests;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

@RunWith(Suite.class)
@Suite.SuiteClasses({ RentalAgreementTests.class, ToolFactoryTests.class, ToolDataStoreTests.class, FormattingToolTests.class })

public class RentalAgreementSuite {
	// the class remains empty,
	// used only as a holder for the above annotations
}
